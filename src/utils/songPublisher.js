// TODO Mock actions and other Babbage SDK functionality.
// 1. Purchase song token via bridgeport action?
// 2. Check for new songs?
// 3. Publish new songs (ACTION)

const songPublisher = (song, bridge) => {
  console.log('Song publish action transaction sent')
}
export default songPublisher
