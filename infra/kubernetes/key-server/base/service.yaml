apiVersion: v1
kind: Service
metadata:
  name: tempo-key-server
  labels:
    app.kubernetes.io/name: tempo-key-server
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: tempo-key-server
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 8081
      targetPort: http
